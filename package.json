{
  "name": "typographix-final",
  "version": "1.0.0",
  "description": "Final version of typographix website.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --source-map scss/main.scss:css/compiled_style.css",
    "sass-prod": "sass --no-source-map --style=compressed scss/main.scss:css/compiled_style.css"
  },
  "author": "Jacinto Design",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
